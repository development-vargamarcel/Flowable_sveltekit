# Dependencies
node_modules

# Build outputs
build
dist
.svelte-kit

# Package files
package-lock.json
pnpm-lock.yaml
yarn.lock

# Config files that shouldn't be formatted
*.min.js
*.min.css

# Svelte files - intentionally excluded after the 2026-05-28 cleanup review because
# repository components still depend on Svelte 5 runes patterns that need careful
# formatter migration. ESLint and svelte-check remain the validation gates.
*.svelte
